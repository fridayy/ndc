/**
 * @author benjamin.krenn@leftshift.one - 7/8/18.
 * @since 1.0.0
 */
import "rxjs-compat/add/observable/empty";
import "rxjs-compat/add/observable/of";
import "rxjs-compat/add/operator/publish";

describe('DispatchResultHandler test', () => {
    test('does what is expected', done => {
        done();
    });
});